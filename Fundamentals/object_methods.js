const obj = {
    name: 'Vikas',
    age: 30
}

// Keys
const key = Object.keys(obj); // [name,age]

// Values
const value = Object.values(obj); //['Vikas', 30]

// Entries
const entries = Object.entries(obj); // [[name, 'Vikas'], [age, 30]]
